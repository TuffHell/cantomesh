[project]
name = "cantomesh"
version = "0.1.0"
description = "Neuro-symbolic Cantonese opera (粵劇) 平仄 versification engine — AIGC core of the CANTOMESH heritage ecosystem."
requires-python = ">=3.9"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
