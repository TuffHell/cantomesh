# Copy to .env and fill in to enable Claude-backed generation.
# Without this key the engine runs in offline "assess-only" mode.
ANTHROPIC_API_KEY=

# Optional: override the default model used for verse generation.
# CANTOMESH_MODEL=claude-sonnet-4-6
